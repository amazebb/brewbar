## Homebrew Packages

This is a snapshot of personal homebrew formula and casks viewable and
searchable via GitHub pages. 

To refresh the `html/packages.tsv` simply run the file `./brewinfo`

```sh
cd html
./brewinfo
```
